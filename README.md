# mini-project


🎓 Student Report Card Generator

📌 Description:

This project is a Student Report Card Generator built using Object-Oriented Programming (OOP) in Python. It combines the use of file handling and exception handling to create a simple system that allows users to add student details, calculate grades based on marks, and fetch report cards using roll numbers.


🛠️ Features:

Add new student details (roll number, name, marks).

Calculate and assign grades automatically.

Fetch student report cards using roll numbers.

Store and retrieve data from a text file.

Handle invalid inputs (like incorrect marks, duplicate entries, or file errors) using exception handling.



📂 Technologies Used:

Python

Object-Oriented Programming (Class, Object, Methods)

File Handling (open, read, write)

Exception Handling (try-except)









